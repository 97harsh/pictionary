version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    # Add repo owner as reviewer
    reviewers:
      - "97harsh"
    # Group updates to reduce PR count
    # Note: Major updates are NOT grouped to avoid peer dependency conflicts
    groups:
      # Group all production dependencies together (minor and patch only)
      production-dependencies:
        dependency-type: "production"
        update-types:
          - "minor"
          - "patch"
      # Group all development dependencies together (minor and patch only)
      development-dependencies:
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"
    # Auto-label PRs for easier filtering
    labels:
      - "dependencies"
      - "automated"
    # Increase PR limit if needed
    open-pull-requests-limit: 10
    # Commit message prefix
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"

  # GitHub Actions updates
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    # Add repo owner as reviewer
    reviewers:
      - "97harsh"
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore"
      include: "scope"
